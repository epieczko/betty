# Joiner Mover Leaver Workflows
# See also: artifact_descriptions/joiner-mover-leaver-workflows.md for complete guidance

# The Joiner-Mover-Leaver (JML) Workflows artifact documents automated identity lifecycle management processes for employee onboarding (joiners), role changes (movers), and offboarding (leavers). This a...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact documents automated identity lifecycle workflows for joiners (new hires), movers (role changes), and leavers (terminations) using IAM platforms like Okta, Azure AD, SailPoint, or Saviynt. It specifies HR system integration, provisioning logic, access assignment rules, and deprovisionin

# BEST PRACTICES
# - Automated Provisioning: Automate 90%+ of standard access provisioning, use SCIM 2.0 for SaaS apps, reduce manual ticket hand
# - HR as Source of Truth: Use HRIS (Workday, SuccessFactors) as authoritative source for user attributes, trigger workflows on
# - Immediate Deprovisioning: Disable accounts within 1 hour of termination notification, revoke VPN/SSO access immediately
# - Role-Based Access: Define RBAC role templates by job function, automatically assign based on department/title, minimize
# - Least Privilege: Provision minimum required access, implement time-bound elevated access, require justification for p

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Joiner workflows"
      - "HRIS integration"
      - "Account provisioning"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
