# Caching Tiers
# See also: artifact_descriptions/caching-tiers.md for complete guidance

# The Caching Tiers artifact documents multi-layer caching strategy across browser cache, CDN (CloudFront, Akamai, Cloudflare), application cache (Redis, Memcached), and database cache layers, defining ...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact documents multi-tier caching architecture including browser cache, CDN layer, application cache (Redis/Memcached), and database cache, defining cache policies (TTL, eviction), hit ratio targets (>80%), invalidation strategies, monitoring metrics, and cache warming procedures to optimiz

# BEST PRACTICES
# - Version Control: Store in centralized version control system (Git, SharePoint with versioning, etc.) to maintain comp
# - Naming Conventions: Follow organization's document naming standards for consistency and discoverability
# - Template Usage: Use approved templates to ensure completeness and consistency across teams
# - Peer Review: Have at least one qualified peer review before submitting for approval
# - Metadata Completion: Fully complete all metadata fields to enable search, classification, and lifecycle management

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Browser caching"
      - "CDN caching"
      - "Application cache"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
