# Grpc Proto Files
# See also: artifact_descriptions/grpc-proto-files.md for complete guidance

# gRPC Proto Files define service contracts for gRPC APIs using Protocol Buffers (Protobuf) Interface Definition Language (IDL). These .proto files specify service methods, message types, field definiti...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact defines gRPC service contracts using Protocol Buffers IDL, specifying service interfaces, RPC methods, request/response message types, streaming patterns, and data structures. It enables contract-first API development, cross-language code generation, type-safe communication, and effici

# BEST PRACTICES
# - Proto3 Syntax: - **Always Use Proto3**: Use `syntax = "proto3";` at top of file
# - Field Numbering: - **Never Change Field Numbers**: Field numbers are part of wire format; changing breaks compatibili
# - Schema Evolution: - **Backward Compatibility**: New clients can read old messages
# - Message Design: - **Meaningful Names**: Use clear, descriptive message names (CreateUserRequest, not Req)
# - Field Design: - **Optional Fields**: All fields in proto3 are optional by default

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Protocol Buffers v3 syntax (.proto files)"
      - "Service definitions with RPC methods"
      - "Message types and field definitions"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
