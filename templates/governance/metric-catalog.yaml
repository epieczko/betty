# Metric Catalog
# See also: artifact_descriptions/metric-catalog.md for complete guidance

# The Metric Catalog is a comprehensive registry that documents all observability metrics collected across systems, services, and infrastructure. This artifact standardizes metric naming conventions, de

metadata:
  # Document Control
  version: "1.0.0"  # Semantic versioning (MAJOR.MINOR.PATCH)
  created: "YYYY-MM-DD"  # Date this artifact was created
  lastModified: "YYYY-MM-DD"  # Date of most recent update
  status: "Draft"  # Draft | Review | Approved | Published | Deprecated

  # Ownership & Accountability
  author: "Author Name"  # Primary author of this artifact
  documentOwner: "Owner Role/Name"  # Person/role responsible for maintenance
  classification: "Internal"  # Public | Internal | Confidential | Restricted

  # Approvals
  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      approvalDate: null  # Date of approval (YYYY-MM-DD)

# PURPOSE
# This artifact maintains a centralized registry of all metrics instrumented across applications, services, and infrastructure. It documents metric names, types (counter, gauge, histogram, summary), labels, semantic meaning, data sources, and usage context. The catalog enforces naming conventions (Pro...

# MAIN CONTENT
# Complete the sections below based on your specific artifact needs

# BEST PRACTICES:
# - Prometheus Naming: Use snake_case with base units (seconds not milliseconds, bytes not megabytes), suffix counters with
# - OpenTelemetry Conventions: Follow OTEL semantic conventions for HTTP (http.server.duration), RPC (rpc.server.duration), databas
# - Metric Type Selection: Use Counter for monotonically increasing values, Gauge for up/down values, Histogram for distributio
# - Cardinality Limits: Enforce strict limits on label count (max 10-15 labels) and label value cardinality (max 100-1000 un
# - Label Consistency: Use consistent label names across metrics (service, environment, region vs service_name, env, aws_re

content:
  overview: |
    # Provide a high-level overview of this artifact
    # What is this document about?
    # Why does it exist?
    
  scope:
    inScope:
      - "Prometheus metric naming conventions (snake_case, base units, suffixes like _total, _seconds, _bytes)"
      - "OpenTelemetry semantic conventions for metrics (http.server.duration, db.client.connections.usage)"
      - "Metric types"
      # Add additional in-scope items
    outOfScope:
      - "Item explicitly out of scope"
      # Add additional out-of-scope items

  details: |
    # Provide detailed information specific to this artifact type
    # Include all necessary technical details
    # Reference the artifact description for required sections
    
# QUALITY CHECKLIST
# Before finalizing, verify:
# ✓ Completeness: All required sections present and adequately detailed
# ✓ Accuracy: Information verified and validated by appropriate subject matter experts
# ✓ Clarity: Written in clear, unambiguous language appropriate for intended audience
# ✓ Consistency: Aligns with organizational standards, templates, and related artifacts
# ✓ Currency: Based on current information; outdated content removed or updated

relatedDocuments:
  - type: "Related Artifact Type"
    path: "path/to/related/artifact"
    relationship: "depends-on | references | supersedes | implements"

changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
