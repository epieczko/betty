# Scaling Policies
# See also: artifact_descriptions/scaling-policies.md for complete guidance

# The Scaling Policies artifact defines auto-scaling configuration for Kubernetes HPA/VPA, AWS Auto Scaling Groups, Azure VMSS, or GCP Managed Instance Groups, documenting target utilization thresholds ...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact documents auto-scaling policies for Kubernetes (HPA/VPA), AWS (Auto Scaling Groups), Azure (VMSS), or GCP (MIG), defining target metrics (CPU 70%, memory 80%, custom metrics), scale-up/scale-down thresholds, cooldown periods, rate limits, and scheduled scaling to automatically adjust c

# BEST PRACTICES
# - Version Control: Store in centralized version control system (Git, SharePoint with versioning, etc.) to maintain comp
# - Naming Conventions: Follow organization's document naming standards for consistency and discoverability
# - Template Usage: Use approved templates to ensure completeness and consistency across teams
# - Peer Review: Have at least one qualified peer review before submitting for approval
# - Metadata Completion: Fully complete all metadata fields to enable search, classification, and lifecycle management

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Kubernetes HPA"
      - "Kubernetes VPA"
      - "Kubernetes Cluster Autoscaler"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
