# Automated Quality Gates
# See also: artifact_descriptions/automated-quality-gates.md for complete guidance

# The Automated Quality Gates artifact defines enforceable quality criteria and automated checks integrated into CI/CD pipelines using tools such as SonarQube, Code Climate, CodeQL, Snyk, Dependabot, an...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact serves as the definitive specification of automated quality enforcement criteria integrated into CI/CD pipelines, defining pass/fail thresholds for code quality, test coverage, security vulnerabilities, performance benchmarks, and compliance checks. It ensures consistent quality standa

# BEST PRACTICES
# - Version Control: Store in centralized version control system (Git, SharePoint with versioning, etc.) to maintain comp
# - Naming Conventions: Follow organization's document naming standards for consistency and discoverability
# - Template Usage: Use approved templates to ensure completeness and consistency across teams
# - Peer Review: Have at least one qualified peer review before submitting for approval
# - Metadata Completion: Fully complete all metadata fields to enable search, classification, and lifecycle management

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Code coverage gates (minimum 80% line coverage, 70% branch coverage)"
      - "SonarQube quality gates (reliability rating, security rating, maintainability ra"
      - "Static code analysis rules (cyclomatic complexity, code duplication, code smells"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
