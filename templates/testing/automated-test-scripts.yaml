# Automated Test Scripts
# See also: artifact_descriptions/automated-test-scripts.md for complete guidance

# The Automated Test Scripts artifact comprises executable test code implementing unit tests (JUnit, Jest, pytest, RSpec), integration tests, end-to-end tests (Selenium WebDriver, Cypress, Playwright, T...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact serves as the executable test automation codebase providing automated validation of application functionality, performance, security, and accessibility requirements. Scripts execute continuously in CI/CD pipelines to catch defects early, prevent regressions, and enable rapid feedback f

# BEST PRACTICES
# - Version Control: Store in centralized version control system (Git, SharePoint with versioning, etc.) to maintain comp
# - Naming Conventions: Follow organization's document naming standards for consistency and discoverability
# - Template Usage: Use approved templates to ensure completeness and consistency across teams
# - Peer Review: Have at least one qualified peer review before submitting for approval
# - Metadata Completion: Fully complete all metadata fields to enable search, classification, and lifecycle management

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Unit tests (JUnit 5, Jest, pytest, RSpec, NUnit, xUnit) - 70% of test pyramid"
      - "Integration tests (Spring Test, Testcontainers, MockMvc) - 20% of test pyramid"
      - "End-to-end tests (Selenium WebDriver, Cypress, Playwright, TestCafe) - 10% of te"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
