# Pipeline Definitions
# See also: artifact_descriptions/pipeline-definitions.md for complete guidance

# The Pipeline Definitions artifact is a comprehensive specification for CI/CD (Continuous Integration/Continuous Deployment) pipelines, documenting build, test, security scanning, and deployment automa

metadata:
  # Document Control
  version: "1.0.0"  # Semantic versioning (MAJOR.MINOR.PATCH)
  created: "YYYY-MM-DD"  # Date this artifact was created
  lastModified: "YYYY-MM-DD"  # Date of most recent update
  status: "Draft"  # Draft | Review | Approved | Published | Deprecated

  # Ownership & Accountability
  author: "Author Name"  # Primary author of this artifact
  documentOwner: "Owner Role/Name"  # Person/role responsible for maintenance
  classification: "Internal"  # Public | Internal | Confidential | Restricted

  # Approvals
  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      approvalDate: null  # Date of approval (YYYY-MM-DD)

# PURPOSE
# This artifact documents CI/CD pipeline configurations including pipeline YAML definitions (Jenkinsfile, .gitlab-ci.yml, .github/workflows/*.yml), stage specifications, job dependencies, quality gates, deployment strategies, and environment configurations. It enables DevOps teams to implement automat...

# MAIN CONTENT
# Complete the sections below based on your specific artifact needs

# BEST PRACTICES:
# - Pipeline as Code: Store all pipeline definitions in version control alongside application code, enable code review for
# - Fail Fast: Run fast tests and checks early in pipeline, fail quickly on errors to provide rapid feedback
# - Idempotency: Design pipeline stages to be idempotent and rerunnable without side effects
# - Modular Design: Break pipelines into reusable stages/jobs, use shared libraries (Jenkins shared libraries, GitHub Ac
# - Security First: Integrate security scanning at every stage (SAST, DAST, SCA, secrets scanning), fail on critical vul

content:
  overview: |
    # Provide a high-level overview of this artifact
    # What is this document about?
    # Why does it exist?
    
  scope:
    inScope:
      - "Pipeline configuration files"
      - "Stage definitions"
      - "Build jobs"
      # Add additional in-scope items
    outOfScope:
      - "Item explicitly out of scope"
      # Add additional out-of-scope items

  details: |
    # Provide detailed information specific to this artifact type
    # Include all necessary technical details
    # Reference the artifact description for required sections
    
# QUALITY CHECKLIST
# Before finalizing, verify:
# ✓ Completeness: All required sections present and adequately detailed
# ✓ Accuracy: Information verified and validated by appropriate subject matter experts
# ✓ Clarity: Written in clear, unambiguous language appropriate for intended audience
# ✓ Consistency: Aligns with organizational standards, templates, and related artifacts
# ✓ Currency: Based on current information; outdated content removed or updated

relatedDocuments:
  - type: "Related Artifact Type"
    path: "path/to/related/artifact"
    relationship: "depends-on | references | supersedes | implements"

changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
