# Sequence Diagrams
# See also: artifact_descriptions/sequence-diagrams.md for complete guidance

# Sequence Diagrams are dynamic modeling artifacts that visualize time-ordered interactions between components, services, actors, and systems to accomplish specific use cases or business processes. Usin...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact documents time-ordered interactions using UML 2.5 Sequence Diagrams showing participants (actors, systems, components, services), messages (synchronous calls, asynchronous messages, return values), lifelines, activation boxes, and timing constraints. Created using PlantUML, Mermaid, Lu

# BEST PRACTICES
# - Version Control: Store in centralized version control system (Git, SharePoint with versioning, etc.) to maintain comp
# - Naming Conventions: Follow organization's document naming standards for consistency and discoverability
# - Template Usage: Use approved templates to ensure completeness and consistency across teams
# - Peer Review: Have at least one qualified peer review before submitting for approval
# - Metadata Completion: Fully complete all metadata fields to enable search, classification, and lifecycle management

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Use case scenarios"
      - "API interaction sequences"
      - "Synchronous patterns"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
