# Architecture Approvals
# See also: artifact_descriptions/architecture-approvals.md for complete guidance

# The Architecture Approvals artifact documents formal decisions and authorizations from the Architecture Review Board (ARB) regarding architectural proposals, design patterns, technology selections, an...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact provides authoritative record of ARB decisions, enabling enforcement of architectural governance, tracking of approved exceptions, and maintaining accountability for architectural choices. It supports audit trails for compliance, provides decision history for future reference, and comm

# BEST PRACTICES
# - ADR Repository Management: Maintain ADRs in version-controlled repository (Git docs/adr/ directory) with sequential numbering (
# - Standardized ADR Template: Use consistent format - Title, Status, Context, Decision, Consequences, Compliance (references to st
# - RACI Clarity: Define explicit RACI matrix for each decision type; avoid multiple Accountable roles (only one A per
# - Timely Documentation: Create ADR within 48 hours of ARB decision while context is fresh; link to meeting minutes and appro
# - Decision Traceability: Link ADRs to architecture artifacts (C4 diagrams, component designs), requirements, and related deci

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "ARB meeting decisions with approval/rejection rationale"
      - "Architecture Decision Records (ADRs) for significant choices using standard temp"
      - "RACI matrix for each decision type (who is Responsible, Accountable, Consulted, "
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
