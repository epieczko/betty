# Physical Architecture Diagram
# See also: artifact_descriptions/physical-architecture-diagram.md for complete guidance

# The Physical Architecture Diagram is a critical technical artifact that documents the actual deployment topology, infrastructure components, network configuration, and physical/virtual resource alloca...

metadata:
  version: "1.0.0"
  created: "YYYY-MM-DD"
  lastModified: "YYYY-MM-DD"
  status: "Draft"
  author: "Author Name"
  documentOwner: "Owner Name/Role"
  classification: "Internal"

  approvers:
    - name: "Approver Name"
      role: "Approver Role"
      date: null

# PURPOSE
# This artifact documents the physical deployment architecture using UML Deployment Diagrams, C4 Deployment diagrams, or cloud-specific architecture diagrams (AWS Architecture Diagrams, Azure Architecture Icons, GCP Architecture Diagramming Tool). Created using Lucidchart, draw.io, CloudCraft, or diag

# BEST PRACTICES
# - Version Control: Store in centralized version control system (Git, SharePoint with versioning, etc.) to maintain comp
# - Naming Conventions: Follow organization's document naming standards for consistency and discoverability
# - Template Usage: Use approved templates to ensure completeness and consistency across teams
# - Peer Review: Have at least one qualified peer review before submitting for approval
# - Metadata Completion: Fully complete all metadata fields to enable search, classification, and lifecycle management

# Main content - customize based on artifact type
content:
  summary: |
    Provide overview and context for this artifact.
  
  scope:
    included:
      - "Physical and virtual server topology"
      - "Network architecture"
      - "Storage infrastructure"
    
    excluded:
      - "Items explicitly out of scope"
  
# Related artifacts
relatedArtifacts:
  - type: "Related Artifact Type"
    path: "path/to/artifact"
    relationship: "depends-on | references | implements"

# Change history
changeHistory:
  - version: "1.0.0"
    date: "YYYY-MM-DD"
    author: "Author Name"
    changes: "Initial version"
