name: language.objectivec
version: 0.1.0
description: Fluency in Objective-C programming language, encompassing message passing,
  dynamic runtime, categories, protocols, blocks, automatic reference counting, key-value
  coding, key-value observing, and Foundation framework. Mastery of the runtime system,
  method swizzling, associated objects, toll-free bridging, and Cocoa design patterns.
  Understanding of manual memory management, retain/release cycles, and interoperability
  with C and Swift.
inputs:
- code_file
- validation_type
outputs:
- language_metadata.json
- validation_report.json
status: active
permissions:
- filesystem:read
entrypoints:
- command: /language/objectivec
  handler: language_objectivec.py
  runtime: python
  description: Fluency in Objective-C programming language, encompassing message passing,
    dynamic runtime, categori
artifact_metadata:
  produces:
  - type: language-metadata
  - type: code-analysis
