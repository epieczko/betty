name: language.clojure
version: 0.1.0
description: Fluency in Clojure programming language, encompassing functional programming,
  immutability, persistent data structures, REPL-driven development, Lisp syntax with
  homoiconicity, macros, multimethods, protocols, transducers, and software transactional
  memory. Mastery of sequence abstraction, destructuring, spec for data validation,
  and concurrent programming with atoms, refs, agents. Understanding of JVM interoperability
  and code-as-data philosophy.
inputs:
- code_file
- validation_type
outputs:
- language_metadata.json
- validation_report.json
status: active
permissions:
- filesystem:read
entrypoints:
- command: /language/clojure
  handler: language_clojure.py
  runtime: python
  description: Fluency in Clojure programming language, encompassing functional programming,
    immutability, persiste
artifact_metadata:
  produces:
  - type: language-metadata
  - type: code-analysis
