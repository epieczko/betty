name: language.python
version: 0.1.0
description: Fluency in Python programming language, encompassing dynamic typing,
  object-oriented and functional paradigms, comprehensions, decorators, context managers,
  generators, async/await concurrency, duck typing, and Pythonic idioms. Mastery of
  Python's data model including dunder methods, metaclasses, descriptors, and the
  import system. Understanding of memory management, GIL implications, and performance
  optimization strategies.
inputs:
- code_file
- validation_type
outputs:
- language_metadata.json
- validation_report.json
status: active
permissions:
- filesystem:read
entrypoints:
- command: /language/python
  handler: language_python.py
  runtime: python
  description: 'Fluency in Python programming language, encompassing dynamic typing,
    object-oriented and functional '
artifact_metadata:
  produces:
  - type: language-metadata
  - type: code-analysis
