name: language.sql
version: 0.1.0
description: Fluency in SQL programming language, encompassing relational algebra,
  query optimization, joins, subqueries, window functions, common table expressions,
  indexes, transactions, isolation levels, and normalization. Mastery of advanced
  query patterns including recursive CTEs, pivot operations, aggregate functions with
  grouping, and query execution plans. Understanding of ACID properties, locking mechanisms,
  and database-specific SQL dialects.
inputs:
- code_file
- validation_type
outputs:
- language_metadata.json
- validation_report.json
status: active
permissions:
- filesystem:read
entrypoints:
- command: /language/sql
  handler: language_sql.py
  runtime: python
  description: Fluency in SQL programming language, encompassing relational algebra,
    query optimization, joins, sub
artifact_metadata:
  produces:
  - type: language-metadata
  - type: code-analysis
